---
const items = Astro.props.items ?? ['Unity', 'C#', 'Movement', '3C'];
const line = items.join(' · ') + ' · ';
const repeats = 8;
---
<div class="marquee"><div class="marquee__in">{Array.from({ length: repeats }).map(() => <span>{line}</span>)}</div></div>
